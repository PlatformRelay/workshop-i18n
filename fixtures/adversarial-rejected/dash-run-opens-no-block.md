---
slideId: rej-dash-run
layout: statement
heading: A four-dash separator opens no frontmatter block
---

The underline below splits the slide, but Slidev opens no block after it, so the
slide that follows can never be given an identity.

Heading spelled with a dash underline
----

Body of the slide nobody can address.
