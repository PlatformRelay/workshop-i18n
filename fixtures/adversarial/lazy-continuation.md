---
slideId: adv-lazy
layout: statement
heading: CommonMark laziness inside containers
---

> a quoted line
lazy continuation at column zero
> and back inside the quote

- a bullet whose text wraps
  onto an indented second line
then continues lazily at column zero

- a second bullet
lazy from the very first continuation

1. an ordered item
   indented continuation
lazy tail

<!--
Speaker: notes in the real deck do this constantly.

- Enforced by the CNI. `kubectl apply` accepts the object even if the CNI ignores it
  (a silent no-op); the lab self-tests enforcement before relying on it.
CKx tie-in: CKA & CKAD Services & Networking.
-->
