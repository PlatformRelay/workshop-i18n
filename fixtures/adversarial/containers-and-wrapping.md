---
slideId: adv-containers
layout: default
heading: Blockquotes, nested lists and wrapped prose
---

> A quoted paragraph that wraps across
> two lines and must keep its marker.

> - a quoted bullet
> - and another

- an item whose text wraps
  onto a second line
- an item with children
  - nested one wrapping
    onto its own second line
  - nested two

1. an ordered item
2. a second ordered item
   with a wrapped continuation

- item followed by a nested quote

  > quoted inside a list item
  > wrapping too

A top-level paragraph that wraps
across three lines
without any container prefix at all.

<!--
Speaker: the `> ` and the two-space indent are skeleton, not prose.
-->
