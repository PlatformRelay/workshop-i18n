# Containers and laziness

<!-- labId: containers-and-lazy -->

> A blockquote whose second line keeps the marker
> and wraps like this.

> A blockquote whose second line drops the marker entirely
and continues lazily at column zero.

>
> An empty quote line above this one.
>

- A list item whose paragraph wraps
  with two-space indentation.
- A list item whose paragraph wraps
but drops the indentation entirely.

1. An ordered item.
2. Another ordered item that wraps
   with three-space indentation.

- Outer item

  > A blockquote nested inside a list item
  > across two lines.

  - A nested bullet
    wrapping once.

A paragraph with no container at all
that simply wraps across
three separate lines.
