# CRLF and BOM

<!-- labId: crlf-and-bom -->

This file uses CRLF line endings throughout.

> A blockquote that wraps
> across two CRLF lines.

```bash
kubectl get pods
```

- A list item that wraps
  with CRLF indentation.
